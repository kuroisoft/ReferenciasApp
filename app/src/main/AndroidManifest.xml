<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bash.app.referenciasapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoBar">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LibroActivity"
            android:label="@string/activity_libro" />
        <activity
            android:name=".VideoActivity"
            android:label="@string/activity_you" />
        <activity
            android:name=".WikipediaActivity"
            android:label="@string/activity_wiki" />
        <activity
            android:name=".FichaActivity"
            android:label="@string/app_name" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AcercaDeActivity"></activity>
    </application>

</manifest>